{
  "name": "PACKAGE_NAME",
  "version": "0.1.0",
  "description": "PACKAGE_DESC",
  "author": {
    "name": "Patrick Dubroy",
    "email": "pdubroy@gmail.com",
    "url": "http://dubroy.com"
  },
  "license": "MIT",
  "main": "main.js",
  "dependencies": {},
  "devDependencies": {
    "browserify": "^5.9.1",
    "jshint": "^2.5.2",
    "tape": "^2.13.4",
    "watchify": "^1.0.1"
  },
  "scripts": {
    "init-repo": "mkdir dist lib third_party",
    "build": "browserify -s NAME -o dist/NAME-bundle.js main.js",
    "watch": "watchify -s NAME -o dist/NAME-bundle.js main.js",
    "test": "tape test/*.js",
    "prepublish": "npm run build && npm run test && npm run lint",
    "lint": "jshint *.js lib/*.js test/*.js"
  }
}
